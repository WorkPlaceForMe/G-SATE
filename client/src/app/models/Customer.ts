export interface Customer {
  id: string;
  datasetName: string;
  emailAddress: string;
  date: any;
  model: string;
  overfit_mode: string;
  version: number;
  data: any;
  //The ? is for make it optionals
}
